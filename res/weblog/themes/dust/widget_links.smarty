{foreach from=$links item=item key=key}
<h2 class="r_title">{$item.category}</h2>
<div class="r_item">
{foreach from=$item.links item=link key=link_md5}
<a href="{$link.url}" rel="nofollow" target="_blank">{$link.title}</a><br />
{/foreach}
</div>
{/foreach}